#include <bits/stdc++.h>
#include <iostream>

using namespace std;

int main()
{
	int limit;
	while(cin >> limit){
    char command[limit];
    for(int i=0; i<limit; i++){
      cin >> command[i];
    }
    for(int i=0; i<limit; i++){

    }
	}
  return 0;
}
